export enum PAGE {
  ADMIN_HOME = "admin_home",
  ADD = "add",
  LOGIN = "login",
  USER_HOME = "user_home",
  USER_CART = "user_cart",
}

export enum SIZE {
  SMALL = "S",
  MEDIUM = "M",
  LARGE = "L",
  XLARGE = "XL",
}
